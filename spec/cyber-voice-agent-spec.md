# SahAI product specification

The canonical product specification supplied by the project owner is stored outside the repository at:

`/Users/root-parth/Downloads/cyber-voice-agent-spec (1).md`

This lightweight pointer avoids silently diverging copies during development. Copy the canonical file here before sharing the repository externally.
